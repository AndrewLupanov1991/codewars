package code;

import com.sun.org.apache.xpath.internal.operations.Or;

import java.math.BigInteger;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.LinkedList;
import java.util.List;

public class Main {

    public static void main(String[] args) {
        System.out.println("Hello");
















      // System.out.println(Meeting.meeting("Fred:Corwill;Wilfred:Corwill;Barney:Tornbull;Betty:Tornbull;Bjon:Tornbull;Raphael:Corwill;Alfred:Corwill"));


        //System.out.println(FindOutlier.find(array));
     //   long l1 = 12345;
        //long l2 = 12345;
       /*System.out.println(Kata.TripleDouble(l1,l2));
        int num = 0;
        Integer[][] mas = new Integer[3][3];
        for (int i = 0; i < mas.length; i++) {
            for (int j = 0; j < mas[i].length;j++) {
                mas[i][j] = ++num;
                System.out.print(mas[i][j]);
            }
            System.out.println();
        }
        System.out.println(mas);
    }

    private int checkHorizontals(int[][] array) {
        for (int i = 0; i < array.length; i++) {
            if (checkAllEquals(array[i])) {
                return array[i][0];
            }
        }
        return -1;
    }

    private int checkVerticals(int[][] array) {
        int[] vert = new int[array.length];
        for (int j = 0; j < array[0].length; j++) {
            for (int i = 0; i < array.length; i++) {
                vert[i] = array[i][j];
            }
            if (checkAllEquals(vert)) {
                return vert[0];
            }
        }

        return -1;
    }

    private boolean checkAllEquals(int... values) {
        if (values.length < 2) {
            return true;
        }
        for (int i = 1; i < values.length; i++) {
            if (values[i - 1] != values[i]) {
                return false;
            }
        }*/
        //return true;
    }

}
